// @flow
import styled from 'styled-components'

// Used to apply optional styles to the child 'ContextMenu'
export const Styler = styled.div`
`

export const Wrapper = styled.div`
  a {
    cursor: pointer;
    user-select: none;
  }
`
